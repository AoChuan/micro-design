server:
  port: 8081
spring:
  application:
    name: micro-design-gateway
  cloud:
    gateway:
      routes:
        - id: tesRoute
          uri: lb://cosumer
          predicates:
            - Path=/tes/find
#          filters:
#
#  cloud:
#    nacos:
#      discovery:
#        server-addr:
#      config:
#        server-addr: ${spring.cloud.nacos.discovery.server-addr:null}
#        namespace: dev
#        group: dev


